// Injected at build time by tsdown (`define`) and by vitest (`define`).
// Declared here so the standalone package typechecks without the upstream
// monorepo's global tooling.
declare const __PKG_VERSION__: string;
